{
  "name": "doableai-trigger-run-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action wrapper for DoableAI trigger-run API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "typescript": "^5.8.3"
  }
}
